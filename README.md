Aqui estamos criando uma função chamada verificar_idade(). Funções servem para organizar o código.
input() serve para receber informação do usuário. O que ele digitar será guardado na variável idade_input.
O comando .isdigit() verifica se o texto digitado contém somente números. Se não for número vai mostra erro return encerra a função (não deixa o programa continuar)
verificar_idade() - Faz o programa rodar.input() Pega a idade do usuário isdigit() Confere se é número int() Converte para número inteiro if / elif / else Decide se pode entrar no evento print() Mostra mensagens ao usuário
